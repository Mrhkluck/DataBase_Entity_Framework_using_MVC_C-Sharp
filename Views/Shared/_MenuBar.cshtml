<div class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        
        @if (User.Identity.IsAuthenticated)
        {
            <li>@Html.ActionLink("Faculty", "Index", "Faculties")</li>
 <!--displaytext,action name,controller name-->
            <li>@Html.ActionLink("Student", "Index", "Students")</li>
            <li>@Html.ActionLink("Standard", "Index", "Standards")</li>
            <li>@Html.ActionLink("Course", "Index", "Courses")</li>
            <li>@Html.ActionLink("Contact", "Contact", "Home")</li>

            <li>Welcome:@User.Identity.Name.ToUpper()</li>
            <li>@Html.ActionLink("Sign Out", "SignOut", "Account")</li>
        }
        else
        {
            <li>@Html.ActionLink("Login", "Login", "Account")</li>
        }
    </ul>
</div>
